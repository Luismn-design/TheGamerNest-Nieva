import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        primary: {
            main: "#1D2D44",
            contrastText: "#fff"
        },
        secondary: {
            main: "#3E5C76",
            contrastText: "#fff"
        }
    },

});


export default theme;
